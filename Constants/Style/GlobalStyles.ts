import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },

  button: {
    flex: 1,
  },

  buttonPressed: {
    opacity: 0.5,
  },
});
